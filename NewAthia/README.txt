This sample is a map for a game I ran for my friends.

The tools at the top modify the map's behavior by adding/removing classes to the page which the CSS settings then enforce on variable locations and location labels.