This sample is a simple game I made after seeing an ad for a similar game.

The code defines and constructs a map and controls the interface for it.

To run, just open MinePerimeterSweeper.html in a browser.